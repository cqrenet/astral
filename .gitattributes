# ---------------------------------------------------------------------------
# Merge strategy overrides for ASTRAL deployments
#
# Files marked merge=ours are customer-owned and must never be overwritten
# by upstream ASTRAL updates.
#
# All other files use auto-merge so upstream improvements flow through cleanly.
# ---------------------------------------------------------------------------
deploy/update-from-upstream.yml merge=ours
reports/definitions.local.yml   merge=ours
